name: webant_internship
description: Тестовое задание для стажировки WEBANT

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  flutter_svg: ^2.0.5
  sizer: ^2.0.15
  go_router: ^7.1.1
  equatable: ^2.0.5
  json_annotation: ^4.8.1
  dio: ^5.1.2
  image_picker: ^0.8.7+5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  json_serializable: ^6.6.2
  build_runner: ^2.3.3

flutter:

  uses-material-design: true

  assets:
    - assets/