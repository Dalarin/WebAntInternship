enum Status { initial, loading, success, failure }

enum ErrorEnum {
  badRequest,
  forbidden,
  notFound,
  conflict,
  unauthorized,
  serverUnavailable,
  serverTemporaryUnavailable,
  noInternetConnection,
  duplicate,
  timeOut,
  unknown
}
